name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      tag:
        description: "Release tag to create/update, for example v0.5.0"
        required: true
      notes:
        description: "Plain-English release notes for users; 2-4 short lines, no changelog-only markdown"
        required: false
      vscode_notes:
        description: "Optional VS Code-specific notes for the extension updater feed (falls back to the shared notes)"
        required: false

permissions:
  contents: write

concurrency:
  group: release-${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.ref_name }}
  cancel-in-progress: false

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v6
        with:
          node-version: "22"
          cache: "npm"

      - uses: actions/setup-java@v5
        with:
          distribution: "temurin"
          java-version: "21"

      - name: Install dependencies
        run: npm ci

      - name: Build language package
        run: npm run build --workspace language

      - name: Resolve release tag
        id: tag
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            tag="${{ inputs.tag }}"
          else
            tag="${GITHUB_REF_NAME}"
          fi
          echo "tag=${tag}" >> "$GITHUB_OUTPUT"

      - name: Ensure release tag
        env:
          TAG: ${{ steps.tag.outputs.tag }}
        run: |
          if ! git rev-parse -q --verify "refs/tags/${TAG}" >/dev/null; then
            git tag "${TAG}" "${GITHUB_SHA}"
            git push origin "refs/tags/${TAG}"
          fi

      - name: Resolve release notes
        id: notes
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
          TAG: ${{ steps.tag.outputs.tag }}
          INPUT_NOTES: ${{ github.event.inputs.notes || '' }}
        run: |
          notes="${INPUT_NOTES}"
          if [ -z "${notes//[[:space:]]/}" ]; then
            notes="$(gh release view "${TAG}" --json body --jq '.body // ""' 2>/dev/null || true)"
          fi
          if [ -z "${notes//[[:space:]]/}" ]; then
            echo "::error::Release notes are required. Use 2-4 plain-English lines that describe the important user-visible changes."
            exit 1
          fi

          {
            echo "notes<<EOF"
            echo "${notes}"
            echo "EOF"
          } >> "$GITHUB_OUTPUT"

      - name: Stage release artifacts
        env:
          HTSW_RELEASE_TAG: ${{ steps.tag.outputs.tag }}
          HTSW_RELEASE_NOTES: ${{ steps.notes.outputs.notes }}
          HTSW_RELEASE_NOTES_VSCODE: ${{ github.event.inputs.vscode_notes || '' }}
        run: python publish.py --no-upload

      # Publishes the staged feeds to the autoupdater VM through a deploy key
      # whose authorized_keys entry forces ~/bin/htsw-deploy on the server —
      # the key can install feed files under /var/www/htsw and nothing else.
      - name: Publish autoupdater feeds
        id: deploy
        env:
          DEPLOY_KEY: ${{ secrets.HTSW_DEPLOY_KEY }}
          DEPLOY_HOST: ${{ secrets.HTSW_DEPLOY_HOST }}
          DEPLOY_USER: ${{ secrets.HTSW_DEPLOY_USER }}
          DEPLOY_KNOWN_HOSTS: ${{ secrets.HTSW_DEPLOY_KNOWN_HOSTS }}
        run: |
          if [ -z "${DEPLOY_KEY}" ]; then
            echo "::notice::HTSW_DEPLOY_KEY secret not set; skipping autoupdater publish."
            echo "deployed=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          mkdir -p ~/.ssh
          printf '%s\n' "${DEPLOY_KEY}" > ~/.ssh/htsw-deploy
          chmod 600 ~/.ssh/htsw-deploy
          printf '%s\n' "${DEPLOY_KNOWN_HOSTS}" > ~/.ssh/htsw-deploy-known-hosts
          tar -C dist-publish -cf - ct vscode | ssh \
            -i ~/.ssh/htsw-deploy \
            -o IdentitiesOnly=yes \
            -o UserKnownHostsFile=~/.ssh/htsw-deploy-known-hosts \
            "${DEPLOY_USER}@${DEPLOY_HOST}" deploy | tee deploy-output.txt
          # The script's last line reports what actually landed on disk:
          #   installed ct=<version> vscode=<version>
          installed="$(grep -E '^installed ct=' deploy-output.txt | tail -1)"
          echo "ct_live=$(sed -E 's/.*ct=([^ ]+).*/\1/' <<< "${installed}")" >> "$GITHUB_OUTPUT"
          echo "vs_live=$(sed -E 's/.*vscode=([^ ]+).*/\1/' <<< "${installed}")" >> "$GITHUB_OUTPUT"
          echo "deployed=true" >> "$GITHUB_OUTPUT"

      - name: Prepare GitHub release assets
        run: |
          cp dist-publish/ct/latest.json dist-publish/htsw-ct-latest.json
          cp dist-publish/vscode/latest.json dist-publish/htsw-vscode-latest.json

      - name: Create or update release
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ steps.tag.outputs.tag }}
          NOTES: ${{ steps.notes.outputs.notes }}
        run: |
          ct_version="$(jq -r .version ct_module/package.json)"
          vs_version="$(jq -r .version editors/code/package.json)"
          title="HTSW v${ct_version} (VScode v${vs_version})"
          if gh release view "${TAG}" >/dev/null 2>&1; then
            gh release edit "${TAG}" \
              --title "${title}" \
              --notes "${NOTES}"
          else
            gh release create "${TAG}" \
              --title "${title}" \
              --notes "${NOTES}"
          fi

          gh release upload "${TAG}" \
            dist-publish/ct/htsw-ct-*.zip \
            dist-publish/vscode/*.vsix \
            dist-publish/htsw-ct-latest.json \
            dist-publish/htsw-vscode-latest.json \
            --clobber

      # Users update through the VM-backed feeds, not the GitHub release. When
      # the deploy step ran, its read-back of the installed latest.json files
      # is authoritative — a mismatch fails the run. When the deploy was
      # skipped (no secret), fall back to fetching the public feed and warn,
      # so a tagged-but-never-published release can't slip by silently again
      # (v0.6.5 did exactly that). The public fetch is best-effort only:
      # the site sits behind Cloudflare, which blocks GitHub runner IPs.
      - name: Check autoupdater feeds match this release
        env:
          TAG: ${{ steps.tag.outputs.tag }}
          DEPLOYED: ${{ steps.deploy.outputs.deployed }}
          DEPLOY_CT_LIVE: ${{ steps.deploy.outputs.ct_live }}
          DEPLOY_VS_LIVE: ${{ steps.deploy.outputs.vs_live }}
        run: |
          fetch_version() {
            local body
            body="$(curl -sf "$1" || true)"
            if [ -z "${body}" ]; then
              echo unreachable
              return
            fi
            printf '%s' "${body}" | jq -r '.version // "unreachable"'
          }
          ct_repo="$(jq -r .version ct_module/package.json)"
          vs_repo="$(jq -r .version editors/code/package.json)"
          if [ "${DEPLOYED}" = "true" ]; then
            ct_live="${DEPLOY_CT_LIVE:-unreachable}"
            vs_live="${DEPLOY_VS_LIVE:-unreachable}"
          else
            ct_live="$(fetch_version https://legendarygames.dev/htsw/ct/latest.json)"
            vs_live="$(fetch_version https://legendarygames.dev/htsw/vscode/latest.json)"
          fi

          {
            echo "## Autoupdater feeds"
            echo "| surface | repo version | live feed |"
            echo "|---|---|---|"
            echo "| CT module | ${ct_repo} | ${ct_live} |"
            echo "| VS Code extension | ${vs_repo} | ${vs_live} |"
          } >> "$GITHUB_STEP_SUMMARY"

          level="warning"
          if [ "${DEPLOYED}" = "true" ]; then level="error"; fi
          failed=0
          if [ "${ct_live}" = "unreachable" ]; then
            echo "::warning title=CT feed unverifiable::CI could not fetch ct/latest.json; verify manually that it serves ${ct_repo}."
          elif [ "${ct_live}" != "${ct_repo}" ]; then
            echo "::${level} title=CT autoupdater is behind::${TAG} has module version ${ct_repo}, but https://legendarygames.dev/htsw/ct/latest.json serves ${ct_live}. Users running /htsw update will NOT get this release. Manual fix: python publish.py --ct-only"
            failed=1
          fi
          if [ "${vs_live}" = "unreachable" ]; then
            echo "::warning title=VS Code feed unverifiable::CI could not fetch vscode/latest.json; verify manually that it serves ${vs_repo}."
          elif [ "${vs_live}" != "${vs_repo}" ]; then
            echo "::${level} title=VS Code updater is behind::editors/code is at ${vs_repo}, but https://legendarygames.dev/htsw/vscode/latest.json serves ${vs_live}. Manual fix: python publish.py --vscode-only"
            failed=1
          fi
          if [ "${DEPLOYED}" = "true" ] && [ "${failed}" = "1" ]; then exit 1; fi
