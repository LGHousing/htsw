// Single switch for import-time diagnostic enrichment. Flip to false to
// silence everything; delete this file and grep `IMPORT_DEBUG` to remove
// the feature entirely.
export const IMPORT_DEBUG = false;
