import type { PaginatedListConfig } from "../menus/paginatedList";

export const ACTION_LIST_CONFIG: PaginatedListConfig = {
    label: "action",
    emptyPlaceholderName: "No Actions!",
};

export const CONDITION_LIST_CONFIG: PaginatedListConfig = {
    label: "condition",
    emptyPlaceholderName: "No Conditions!",
};
